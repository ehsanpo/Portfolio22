import React from "react";
import { StaticQuery, graphql } from "gatsby";
import "./style.scss";

const ParallaxImage = ({
	filename,
	alt,
	style = {},
	className = "",
	caption = null,
}) => <div></div>;

export default ParallaxImage;
